Read the current project structure carefully.

Convert this entire Sentence Calculator project into a React (Vite) application.

Requirements:
1. Keep UI exactly same as current version.
2. Convert all HTML into React components (App.jsx + reusable components).
3. Convert all JavaScript logic into React hooks (useState, useEffect if needed).
4. Replace DOM manipulation with React state updates.
5. Move CSS into App.css or component-level CSS.
6. Create proper folder structure:
   src/
     components/
     App.jsx
     main.jsx
     App.css

7. Ensure the app runs without errors using Vite React setup.
8. Preserve all existing functionality of the sentence calculator.

If any file is missing or logic is unclear, infer it from existing code and implement it in React way.
